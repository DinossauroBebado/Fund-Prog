#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
struct equipamento{
  char nome[50];
  float potencia,tempo_ativo;
};

char fim[1];
float comsumo_mens
int main()
{
    struct equipamento equip[5] ;
    for(int i = 0; i<5 ; i++){
        printf("Nome do eletrodomestico:");
        fflush(stdin);
        gets(equip[i].nome);

        printf("Potencia:");
        scanf("%f",&equip[i].potencia);

        printf("Tempo que o aparelho fica ligado:");
        scanf("%f",&equip[i].tempo_ativo);

        //printf("Tem mais algum eletrodomestico ? (S/N) \n");
        
        }

    return 0;
}





